<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused state (cuando tiene focus) -->
    <item android:color="@color/focusedBorder" android:state_focused="true" />
    <!-- Default state (cuando no tiene focus) -->
    <item android:color="@color/unfocusedBorder" />
</selector>